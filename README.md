# FreshCart-Two-Tier-Architecture
AWS two-tier cloud architecture capstone — custom VPC across 2 AZs, backend EC2 instances in private subnets behind an Application Load Balancer, NAT Gateway for outbound-only access, and a CloudFront + S3 static site with HTTPS and a lifecycle policy. Fully scripted via AWS CLI.
